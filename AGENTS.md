# AGENTS

## Gestor de paquetes
- Este proyecto usa **pnpm** (existen `pnpm-lock.yaml` y `pnpm-workspace.yaml`). No usar npm ni yarn.
- Comandos: `pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm test`.
- Nota: el script `test` usa vitest, pero la dependencia aún no figura en `devDependencies`.
